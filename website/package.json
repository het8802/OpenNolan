{
  "name": "opennolan-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Landing page + waitlist for the OpenNolan Mac app",
  "scripts": {
    "dev": "npx --yes serve --listen 8787",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {}
}
